import 'package:flutter/material.dart';

// Define a constant color for red used in the application
const Color tdRed = Color(0xFFDA4040);

// Define a constant color for blue used in the application
const Color tdBlue = Color(0xFF5F52EE);

// Define a constant color for black used in the application
const Color tbBlack = Color(0xFF3A3A3A);

// Define a constant color for grey used in the application
const Color tbGrey = Color(0xFF717171);

// Define a constant color for the background color used in the application
const Color tbBGColor = Color(0xFFEEEFF5);
